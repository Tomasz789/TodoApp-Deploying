@using Todo.Domain.Entities
@model TodoList
@{
    ViewData["Title"] = "Remove task list";
}
<div class="col-12 text-center">
    <h1>Remove task List @Model.Title.</h1>
    <form class="border" method="post">
        <h2><span class="text-danger">Caution!&nbsp;</span>Your list will definitely deleted.</h2>
        <p>All Your tasks will be <span class="text-danger">removed!</span></p>
        <p>Continue?</p>
        <button class="btn btn-danger mb-1" asp-action="RemoveConfirmation" asp-controller="List" asp-route-id="@Model.Id">Yes, remove</button>
        <a class="btn btn-success mb-1" asp-controller="List" asp-action="Index">Take me back</a>
    </form>
</div>
